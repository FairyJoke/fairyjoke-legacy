{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FairyJoke",
            "type": "shell",
            "command": "PYTHONPATH=src ./.venv/bin/python -m src.fairyjoke",
        },
        {
            "label": "Init data",
            "type": "shell",
            "command": "PYTHONPATH=src ./.venv/bin/python -m src.fairyjoke init",
        },
        {
            "label": "Prepare data",
            "type": "shell",
            "command": "PYTHONPATH=src ./.venv/bin/python -m src.fairyjoke prepare",
        }
    ]
}